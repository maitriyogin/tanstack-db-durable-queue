{
  "name": "tanstack-db-durable-queue",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "bff-todos",
    "fe-todos",
    "fe-todos-redux"
  ],
  "scripts": {
    "dev": "concurrently --names bff,fe,redux --prefix-colors blue,green,magenta \"bun run dev:bff\" \"bun run dev:fe\" \"bun run dev:redux\"",
    "dev:bff": "bun run --cwd bff-todos dev",
    "dev:fe": "bun run --cwd fe-todos dev",
    "dev:redux": "bun run --cwd fe-todos-redux dev",
    "build:fe": "bun run --cwd fe-todos build",
    "build:bff": "bun run --cwd bff-todos build",
    "build:redux": "bun run --cwd fe-todos-redux build",
    "test": "bun run --cwd fe-todos test",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "concurrently": "^9.2.1"
  }
}